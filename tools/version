#!/bin/sh

set -eu

query=$(printf '.packages[] | {name, version} | select(.name == "%s") | .version' "${1:-proxydetox}")
printf '::set-output name=version::'
cargo metadata --format-version 1 --no-deps --offline --quiet | jq -r "${query}"
